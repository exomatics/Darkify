import styled from 'styled-components';

export const StyledHeader = styled.div`
  grid-area: header;
  display: flex;
  align-items: center;
  .primary-buttons {
    display: flex;
    align-items: center;
    gap: 10px;
  }
`;
